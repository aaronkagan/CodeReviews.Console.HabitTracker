# Habit Tracker

Console based CRUD application to track various habits.

Developed using C# and SQLite.

# Given Requirements:

-[X] Users need to be able to input the date of the occurrence of the habit
-[X] The application should store and retrieve data from a real database
-[X] When the application starts, it should create a sqlite database, if one isn’t present.
-[X] It should also create a table in the database, where the habit will be logged.
-[X] The users should be able to insert, delete, update and view their logged habit.
-[X] You should handle all possible errors so that the application never crashes.
-[X] You can only interact with the database using ADO.NET. You can’t use mappers such as Entity Framework or Dapper.
-[X] Follow the DRY Principle, and avoid code repetition.



# Challenges
-[X] The ability for the user to add different types of habits to track
-[X] The ability for users to only view records for a specific habit
-[X] Showing users the options for which habits to show
-[X] Using parameterized queries
-[x] Seeding the data the first time with dummy data

# Lessons Learned
- Understanding parameterized queries


# Things I found hard
- Creating raw and parameterized SQL Queries

# Areas to Improve
- UI Improvements


